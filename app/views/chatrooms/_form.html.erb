<%= form_for(@chatroom) do |f| %>
  <% if @chatroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chatroom.errors.count, "error") %> prohibited this chatroom from being saved:</h2>

      <ul>
      <% @chatroom.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :venue_id %><br />
    <%= f.text_field :venue_id, :value => @venue_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, :value => @name.name %>
  </div>
  <div class="field">
    <%= f.label :is_available %><br />
    <%= f.check_box :is_available %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
